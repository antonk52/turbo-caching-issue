{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**"],
      "outputs": ["dist/**"]
    },
    "clean": {
      "inputs": ["dist/**"],
      "dependsOn": [],
      "outputs": [],
      "cache": false
    }
  }
}
